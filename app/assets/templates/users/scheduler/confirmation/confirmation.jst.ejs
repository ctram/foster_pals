<h3>Requests Sent for the Following:</h3>

<% for (var i = 0; i < animals.models.length; i++) { %>
  <% var animal = animals.models[i] %>
  <div>
    <img src=<%= animal.escape('main_image_thumb_url') %>>
    <span class='strong'>
      <%= animal.escape('name') %>
    </span>
  </div>
<% } %>

<button class='btn btn-primary to-map'>
  Return to the Map
</button>
