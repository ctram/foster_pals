<h3>Requests Sent for the Following!</h3>

<% for (var i = 0; i < stays.models.length; i++) { %>
  <% animal = stays.models[i] %>

  
  <div>
    <img src=<%= animal.escape('main_image_thumb_url') %>>
    <%= animal.escape('name') %>
    <%= animal.escape('status') %>
  </div>

<% } %>
